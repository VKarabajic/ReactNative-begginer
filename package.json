{
  "name": "weblica-app-backend-master",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf build && mkdir build",
    "build-babel": "babel -d ./build ./src -s --copy-files",
    "build": "npm run clean && npm run build-babel",
    "start": "npm run build && node ./build/index.js",
    "develop": "node_modules/.bin/nodemon ./src/index.js --exec babel-node --"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "body-parser": "^1.18.3",
    "editorconfig": "^0.15.3",
    "express": "^4.16.4",
    "mysql": "^2.16.0",
    "sequelizer": "^1.1.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
